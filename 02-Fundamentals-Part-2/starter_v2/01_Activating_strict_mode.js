"use strict";

let hasDriversLicense = true;
const passedTest = false;

if (passedTest) {
    /*  hasDriverLicense=true; //making an error to understand strict mode usage.*/
}
if (hasDriversLicense) {
    console.log(`I can drive.`);
}

/*const interface = `Audio`; //the word 'interface' is reserved. strict mode is highlighting here.

const private = 1000; //the word 'private' is reserved. strict mode is highlighting here.*/